#include<bits/stdc++.h>
using namespace std;
void reverse(string str)
{
    if(str.empty()) 
        return;
     reverse(str.substr(1));
     cout<<str[0];   
}
int main()
{
    string str;
    cout<<str;
    reverse(str);
    return 0;
}
