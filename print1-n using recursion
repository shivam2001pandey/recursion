#include <iostream>
using namespace std;

void recur(int n)
{
    if(n==0) return;
    recur(n-1);
    cout<<n<<"";
}
int main() {
	//code
	int t;
	cin>>t;
	while(t--)
	{
	    int n;
	    cin>>n;
	    recur(n);
	    cout<<endl;
	}
	return 0;
}
